# Hataori 3.1.11.0 実機検証

- 実施日: 2026-08-30
- 対象: 起動時Session recoveryの追加堅牢化
- MSI: `Hataori-3.1.11.0-x64.msi`
- SHA-256: `A057A6552BB26D3C5F4D12C158D765D478D3AADCA4FED5D73692FF171CABE6B6`
- WiX Toolset: 5.0.2

## 結果

- Release構成build: 警告0件、エラー0件
- Release構成テスト: 176件合格、失敗0件
- MSI build: 警告0件、エラー0件
- Major Upgrade: `F:\Hataori`の3.1.10.3から3.1.11.0へ更新、終了コード0
- インストール先CLI: 3.1.11.0
- Windows Service: Running / Automatic
- MCP接続: 接続済み、19ツール

## 実装範囲

- Agent Run作成前に残った孤立実行メッセージを、起動時に失敗状態へ復旧する。
- Agent Run完了後かつ返信前のメッセージは、Agentを再実行せず返信のみを再試行する。
- 生存中のAgent processと既存の返信再試行を維持する。

## 実機で発生した事象と対応

- 非昇格での初回Upgradeは、旧版削除時にWindows Installerエラー1730となり、全体が1603でロールバックされた。
- UAC昇格したWindows Installerで再実行し、終了コード0でMajor Upgradeを完了した。
- `hataori doctor`はconfiguration、MCP、SQLite、Claude CLI、Windows Service、hooksが合格した。serverとItogurumaはServiceと同一のSYSTEMアカウントが必要なため想定どおりSkippedとなった。
- Codex CLIは既存設定が参照するWindowsApps内の旧実行ファイルが存在せず不合格となった。今回の変更とは無関係の既存環境事象である。
- 本番Installの保持を優先し、Uninstall検証は延期した。
