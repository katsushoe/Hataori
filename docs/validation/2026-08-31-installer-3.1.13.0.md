# Hataori 3.1.13.0 実機検証

- 実施日: 2026-08-31
- 対象: Workspace単位のTask管理とProject候補検索
- MSI: `Hataori-3.1.13.0-x64.msi`
- SHA-256: `8580FF33483FBF4A918A2EAA741727E215FFA5774F4CAF7424B51BA3172A04DB`
- WiX Toolset: 5.0.2

## 結果

- Release構成build: 警告0件、エラー0件
- Release構成テスト: 189件合格、失敗0件
- MSI build: 警告0件、エラー0件
- Major Upgrade: `F:\Hataori`へインストール、終了コード0
- インストール先CLI: 3.1.13.0
- Windows Service: Running / Automatic
- MCP接続: 接続済み、24ツール
- MCP実機確認: `list_workspaces`、`list_projects`、Workspace単位のTask登録・一覧・競合検索ツールを確認

## 実装範囲

- Workspace IDをTaskとSQLiteへ追加し、既存データを`default`へ移行する。
- `list_workspaces`とWorkspace単位のTask MCPツールを追加する。
- Server Instructions、MCP Prompt、会話Hookで`list_workspaces`から`list_projects`の順に候補確認を案内する。
- Monitor TaskへWorkspace IDを追加する。

## 実機で確認した事項

- 既存設定では`activation.workingDirectory`が空のため、`list_workspaces`は`default`を返し、Project候補は空配列となった。未設定時に例外を返さない設計どおりの結果である。
- `hataori doctor`はconfiguration、MCP、SQLite、Claude CLI、Windows Service、hooksが合格した。serverとItogurumaはServiceと同一のSYSTEMアカウントが必要なため想定どおりSkippedとなった。
- Codex CLIは既存設定が参照するWindowsApps内の旧実行ファイルが存在せず不合格となった。今回の変更とは無関係の既存環境事象である。
- 本番Installの保持を優先し、Uninstall検証は延期した。
